# Provisioned automatically by `vercel integration add neon` + `vercel env pull`.
DATABASE_URL=

# From Walmart Seller Center (seller.walmart.com) -> Account -> API key management.
WALMART_CLIENT_ID=
WALMART_CLIENT_SECRET=

# From a self-authorized SP-API app (Seller Central > Apps & Services >
# Develop apps). See docs/amazon-connector-plan.md - unverified, only
# used by scripts/test-amazon-connection.ts.
AMAZON_LWA_CLIENT_ID=
AMAZON_LWA_CLIENT_SECRET=
AMAZON_REFRESH_TOKEN=

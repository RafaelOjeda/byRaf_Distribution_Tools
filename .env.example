# Provisioned automatically by `vercel integration add neon` + `vercel env pull`.
DATABASE_URL=

# From the Walmart Developer Portal, tied to your seller account.
WALMART_CLIENT_ID=
WALMART_CLIENT_SECRET=

# Generate any random string. Set the same value in Vercel env vars so
# app/api/cron/sync/route.ts can verify requests actually come from
# Vercel Cron rather than being publicly callable.
CRON_SECRET=
